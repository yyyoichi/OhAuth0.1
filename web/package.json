{
	"name": "web",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"mock": "export NEXT_PUBLIC_MODE=mock && next dev",
		"build": "next build",
		"start": "next start",
		"mbuild": "export NEXT_PUBLIC_MODE=mock && next build",
		"mstart": "export NEXT_PUBLIC_MODE=mock && next start",
		"biome": "npx biome check --apply ./src && npx @biomejs/biome format --write ./src"
	},
	"dependencies": {
		"next": "14.1.3",
		"react": "^18",
		"react-dom": "^18"
	},
	"devDependencies": {
		"@biomejs/biome": "1.6.1",
		"@types/node": "^20",
		"@types/react": "^18",
		"@types/react-dom": "^18",
		"autoprefixer": "^10.0.1",
		"postcss": "^8",
		"tailwindcss": "^3.3.0",
		"typescript": "^5"
	}
}
